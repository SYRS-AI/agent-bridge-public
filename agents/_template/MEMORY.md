# Memory

## Current State
- 아직 장기 메모가 없다.

## Operating Notes
- 반복적으로 필요한 사실, 사람 선호, 결정된 정책은 여기에 요약한다.
- memory 구조와 승격 규칙은 `MEMORY-SCHEMA.md`를 따른다.
- 사용자별 사실은 `users/<user-id>/...` 아래에서 관리한다.
- 공유 기억은 `memory/shared/`, 프로젝트 기억은 `memory/projects/`, 결정은 `memory/decisions/`에 남긴다.
- 기억 위키의 진입점은 `memory/index.md`, 유지보수 로그는 `memory/log.md`다.
